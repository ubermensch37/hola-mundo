name: Release
on:
  release:
    types:
      - published
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  register:
    name: Package, Publish, and Register
    runs-on:
    - ubuntu-latest
    steps:
    - id: checkout
      uses: actions/checkout@v3
    - if: ${{ github.event_name != 'pull_request' || ! github.event.pull_request.head.repo.fork }}
      uses: docker/login-action@v2
      with:
        registry: docker.io
        username: ${{ secrets.DOCKER_HUB_USER }}
        password: ${{ secrets.DOCKER_HUB_PASS }}
    - name: Cache Pack
      id: cache-pack
      uses: actions/cache@v3
      with:
        path: /usr/local/bin/pack
        key: ${{ runner.os }}-pack-${{ hashFiles('**/pack*') }}
    - id: setup-pack
      uses: buildpacks/github-actions/setup-pack@v4.9.0
    - name: build image
      run: pack build ${{ secrets.DOCKER_IMAGE_NAME }} --builder ${{ vars.BUILDER }} --path . 
    - name: Tag Image
      run: docker tag ${{ secrets.DOCKER_IMAGE_NAME }} ${{ secrets.DOCKER_HUB_REPO }}/${{ secrets.DOCKER_IMAGE_NAME }}:${{ github.sha }}   
    - id: echo2
      run: |
        
        docker image ls
        
        echo "repository variable : ${{ github.run_id }}"
         echo "repository variable sha : ${{ github.sha }}"
        echo Builder ${{ vars.BUILDER }}
      shell: bash
      env:
        REPO: docker.io/${{ secrets.DOCKER_HUB_USER }}
    - name: Push Image
      run: docker push ${{ secrets.DOCKER_HUB_REPO }}/${{ secrets.DOCKER_IMAGE_NAME }}:${{ github.sha }}
    - name: Instal IBM Cloud CLI URL
      uses: https://github.com/IBM/actions-ibmcloud-cli
      with:
        APIKEY: ${{ secrets.IBM_USER}}
        CLOUD_REGION: us-south
    - name: Push Image
      run: ibmcloud cr image-push ${{ secrets.DOCKER_HUB_REPO }}/${{ secrets.DOCKER_IMAGE_NAME }}:${{ github.sha }}
